version: "3"

vars:
  PODMAN_VERSION: "4.8.2"


tasks:
  install:
    cmds:
      - installBasics
      - 
  remove:
    cmds:
      - removebasics
  installBasics:
    cmds:
      - sudo apt-get update
      - sudo apt-get -y install nano unzip exa duf bat buildah podman
  removebasics:
    interactive: true
    cmds:
      - sudo apt-get remove nano unzip exa duf bat buildah podman